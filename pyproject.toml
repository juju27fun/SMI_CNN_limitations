[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "p0-particle-benchmark"
version = "0.1.0"
description = "P0 particle classification benchmark utilities"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["p0"]
